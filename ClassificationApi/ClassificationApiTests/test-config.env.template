# Test Configuration Template
# Copy this file to test-config.env and replace the placeholder values with your actual ByteShelf credentials

# ByteShelf API Configuration
# Replace with your actual ByteShelf API key (must be at least 20 characters)
BYTESHELF_API_KEY=your-actual-api-key-here-minimum-20-characters

# ByteShelf base URL (must be at least 10 characters)
BYTESHELF_BASE_URL=https://api.byteshelf.com

# Cache directory for ByteShelf resources
BYTESHELF_CACHE_DIR=TestByteShelfCache

# Note: This file is gitignored to prevent committing sensitive information
# Make sure to replace the placeholder values with your real credentials 